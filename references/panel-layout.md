# Panel Layout

Use semantic ids from `src/matlab_sci_plot/layout.py` and MATLAB `mpBuildLayout`: `single`, `paired`, `triptych`, `hero_plus_diagnostics`, `overview_plus_small_multiples`, `small_multiples`, and `legend_panel`. Reading order is explicit, main evidence may span more area, comparable panels share scales by default, and independent scales require disclosure.
