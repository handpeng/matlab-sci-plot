# Scientific Integrity

Negative R2 remains negative. Direct model comparison uses a common complete sample and records `n_common`. Presentation operations cannot alter observations, metrics, grouping, uncertainty, or sample membership. Inferential/modeling operations require an upstream artifact or explicit Class-C authorization.
