# Typography

The MATLAB backend applies explicit font and size values after final-size resolution. Keep labels in English for the current legacy-compatible templates, use a readable minimum size, and never repair overlap by hiding scientific labels.
