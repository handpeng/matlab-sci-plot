# Semantic Color

Use stable role ids (`observed`, `proposed`, `baseline`, `positive`, `negative`, `uncertainty`, `missing`) resolved by `style.py`. Categorical roles use Okabe-Ito colors; ordered values use a perceptually ordered sequential palette and signed quantities a diverging palette. `jet`/rainbow are never the default quantitative map.
