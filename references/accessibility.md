# Accessibility

Color is supplemented by marker, line style, direct labels, or panel separation when needed. Check contrast and final-size legibility in the rendered artifact. Missing and out-of-range values use a distinct semantic role and are never silently converted to zero.
