# Uncertainty

An interval must name its semantics: SD, SE, confidence interval, prediction interval, bootstrap interval, posterior interval, cross-validation variation, measurement uncertainty, or model uncertainty. A generic `95% Band` is rejected unless the Figure Contract supplies the method and sample definition.
